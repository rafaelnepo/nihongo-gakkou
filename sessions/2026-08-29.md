# 2026-08-29 — city-pop batch shipped; dashboard + nudge tool overhauled; illustrations everywhere

Previous: [2026-08-27](./2026-08-27.md)

## What happened
Big session. All 20 commits are `208d4c0..e78bbd2`. Grouped:

**Videos — the 10 city-pop cuts are DONE.**
- Ran the 10 master WAVs from `_INBOX/` through the pipeline: copy → `build_learning_timing` → WhisperX align → registry → sync → render (`f71b999`). All 10 rows (`01-aiueo` … `10-wawon-female`) have landscape + vertical renders in `video/out/<id>/`.
- Fixed a scaffold bug: aligned songs still showed the `placeholder timing · align for real sync` watermark — the aligner now clears `placeholder` (`f71b999`).

**Illustrations — all 30 songs, plus the fix that mattered.**
- Curated Irasutoya art for all 10 videos + the 20 variant songs (`307fd86`, `f092aef`). Picker rebuilt (30/30, 423 words). Picks were auto-chosen via the sibling catalog's `q.py --hybrid` (semantic) with reuse across songs; a handful of abstract words are best-effort.
- **Tail-illustration fix (`6bca8d7`):** bridge/review/outro lines were all scaffolded with a placeholder illos band (first vocab group), so from ~line 36 the illustrations stopped tracking the lyric. Now each tail line shows the vocab words it actually sings (whole-word + trailing-particle match; 1-kana words exact; first-group fallback for pure refrains). Fixed in `tools/build_learning_timing.mjs` (`tailLines`) AND patched into all 10 timing JSONs (touching only `illos`, nudges preserved).
- Study sheets now render each vocab word's thumbnail (`08864d8`); picker thumbnails enlarged 42→80px and moved left beside the kana (`4548beb`, `1f79e01`).

**Offline-safe renders (`7ee49db`).** Vendored Zen Maru Gothic locally (`video/public/fonts/*.ttf`, tracked via a .gitignore exception) and load it through `@remotion/fonts` — renders no longer fetch the font from Google at render time (a DNS blip had failed 6 renders mid-batch).

**Dashboard redesign** (`87cc735`, `b7cc1ec`, `bc0907c`).
- Only the **learning** strand now (personal/yuba dropped). Strand filter → **Script**: Hiragana / Katakana / Kanji (latter two empty, ready for future songs via `song.json` `"script"`).
- Card redesign: title in Klee One (the 曲のダッシュボード face), header `Row N › 〈kana〉` + English theme on its own line, blurb as prose (dropped the redundant "Row N of the gojuon" preamble on all songs), meta moved into an infobox, actions a 2×2 grid, a **Videos** link (relative → opens the local `video/out/<id>/` when viewed locally), and clean titles (stripped the `〈kana〉・` prefix from every `song.json`).
- 01-aiueo gained its missing theme ("a little day").

**The Iconotes Nudger got a big pass** (`852a683`, `ed1debf`, `e3c9130`, `6d58365`, `fff001f`, `d62f3c8`, `e78bbd2`):
- Friendly song labels + **in-place song switching** (no page reload) + ‹ prev / next › buttons.
- **Stopped the page reloading on every save** (Vite was reloading because the registry statically imports the timing JSONs; now `server.watch.ignored` skips `video/timing/`).
- **Cascade fix:** a pushed line now slides forward keeping its duration instead of collapsing to a sliver (fixed in `bakeLines` AND the GUI `bakeWindows`).
- **Clamps:** a line can't be nudged narrower than 0.1s; START can't grow past the previous line's end (the value stops at the real limit).
- **NEW feature — drag to stretch a sound:** each word bar in the Word timeline has a right-edge grip; drag it to hold that sound longer / clip it shorter (`wordLengths[]`, a new overlay alongside `wordShifts[]`). `applyWordShifts`→`applyWordEdits` scales chars into the resized window; preview == render.

Everything pushed and live on Pages (CI green).

## Decisions
- **Timing JSONs are committed** (they carry the alignment + hand nudges — the real artifact). `video/out/**` and `video/public/**` stay gitignored (except the vendored font).
- The **Videos** dashboard link is intentionally **relative** (`video/out/<id>/`), not an absolute `file://` — it opens the local folder when the dashboard is opened locally and simply doesn't resolve on the deployed site (renders aren't uploaded). Avoids leaking a local path into the public page.
- Illos matching prefers **no wrong illustration over completeness**: adjective/compound forms (きいろい, ひゃくえん) fall back to the first-group default rather than mis-match.

## State now
- **10 videos rendered** with the illos fix + tuned timing. Working tree clean EXCEPT one WIP file (below).
- **WIP (uncommitted): `video/timing/03-sashisuseso-gohan.learning.json`** — row 3's hand-tuned timing (done by the user this session). NOT yet committed and NOT yet re-rendered (the batch render of 03 predates the final tuning, so 03's current video is stale).
- Nudge server may still be running on `http://localhost:3010` (started with `npm --prefix video run nudge`). Live site: https://rafaelnepo.github.io/nihongo-gakkou/

## Next (where to pick up)
1. **Row 4 (`04-tachitsuteto-ouchi`) timing tuning** — the user is picking up here. Open the nudger (`cd video && npm run nudge` → localhost:3010), pick "Row 4" from the dropdown, tune with the ◀▶ nudges and the new drag-to-stretch on word bars.
2. When rows are done, **re-render just the tuned rows** and commit their timing. Re-render one song: `cd video && npm run render -- <id> out/<id>/<id>-landscape.mp4` and `-- <id>-vertical …`. **Row 3 is already tuned and needs this** (commit `video/timing/03-…json` + re-render 03) — do it alongside row 4, or whenever.
3. Optional follow-ups noted but not done: handle adjective/compound illos fallbacks (きいろい→きいろ); the 20 variant songs have illustration picks but no audio/timing, so they can't render until Suno takes exist.
