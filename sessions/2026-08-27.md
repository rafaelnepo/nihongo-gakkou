# 2026-08-27 — genesis: songs → Remotion engine → its own published repo

Previous: — (first session)

The project was born this day, inside `Irasutoya/songs/`, and ended the day as
its own published repo with a live site. Long session; this is the map.

## What happened
- **Two personal songs written** (private, family): `te-extrano` (for his wife —
  bachata × Japanese) and `onaji-tsuki` (for his son — a music-box lullaby).
  The lullaby was generated and got a full **sing-along lyric video**.
- **Built the Remotion lyric-video engine** (`video/`): a parametric template
  (zod schema, `calculateMetadata` auto-duration, `@remotion/layout-utils`),
  karaoke per-character light-up + a bouncing red ball, a night-sky background.
  One template renders every song via a registry.
- **Built the WhisperX forced-aligner** (`tools/align/`): known-lyrics alignment →
  per-character timings, with a `debunch` pass for held/sustained notes. Proved on
  the lullaby (199/199 chars anchored).
- **Built the HTML output system**: per-song `sheet.html` **generated** from
  `song.json` + the `.txt` sources by `tools/build_sheets.mjs`, a filterable
  **dashboard** (`index.html`), and a **music glossary** (`reference/`).
- **Designed the first learning song, `01-aiueo`** (gojuon row 1): went from
  words-only (v1) → **contextual** word→sentence (v2) → a **3-minute** teach →
  day-bridge → review cut (v3). Ran a **voice audition** across styles.
- **Split the whole thing out of Irasutoya** into
  `~/Desktop/claude-projects/2-medium-priority/nihongo-gakkou/`, made it a
  standalone repo, and **published**: GitHub repo, GitHub Pages, and a CI Action
  that auto-runs the generator on push.
- **Wrote the Comprehensible Input principles** doc grounding the pedagogy.

## Decisions
- **House voice = city-pop female** (Persona to be made from the winning take).
  koto×male and lo-fi are kept for other/moodier content. (findings F-11)
- **Comprehensible input from song 1** — teach words *in context* (word→sentence),
  never bare lists. (README guiding principle; `reference/comprehensible-input.md`)
- **Duration standard**: ~1:00 vertical (Reels) · ~3:00 YouTube, the long cut
  filled with *sentences* not a loop. (house-style.md; findings F-12)
- **Privacy**: `personal/` (family songs) and `books/` (copyrighted Krashen PDFs)
  are **git-excluded**; the dashboard drops private songs; family names scrubbed
  from public docs; the engine's public demo is a neutral `aiueo-demo`, not the
  family lullaby.

## Key findings logged (see `findings.md`)
- **F-10** — a list of same-onset words *blends*; put words in sentences (also
  better pedagogy = i+1).
- **F-11** — voice audition: city-pop female wins for the basic songs.
- **F-12** — Suno performs pasted lyrics **once**; length is predictable from
  lyric quantity, so reach 3 min by adding a bridge+review, not a loop.

## State now
- **Live:** https://rafaelnepo.github.io/nihongo-gakkou/ · repo
  https://github.com/rafaelnepo/nihongo-gakkou · CI green, Pages serving.
- **Songs:** `01-aisatsu` generated (bespoke sheet); `02-gojuon` written (bespoke
  sheet); `01-aiueo` audition done, **v3 written but not generated**; both personal
  songs written (`onaji-tsuki` has a shipped video). `yuba/` empty.
- **Engine:** proven on the lullaby (the lullaby template). Learning-video features
  (word→sentence highlight, illustration slot) **not yet built**.
- `assets/` (sting + sign-off) still **empty**.

## Next (where to pick up) — NEXT SESSION = the AIUEO music-video VISUALS
The immediate focus is building the **learning-video visuals for `01-aiueo`**.
Read `video-format.md` first — it is the spec, and the rules are firm:

1. **Extend the video template for learning videos** (the headline task). Per
   `video-format.md`: **no explanations on screen** (immersion — no translation,
   romaji, furigana, or gloss). Add **word → sentence** presentation and an
   **illustration slot per word** (pull illustrations from the sibling Irasutoya
   project's `q.py` — README → External dependency), keeping the karaoke ball +
   highlight. The current engine is the *lullaby* template; this is the learning
   variant. The `01-aiueo` `screen.txt`, `song.json` vocab, and the day/bridge/
   review structure are all ready to drive it.
2. **The word list ("What it teaches") stays OUT of the video** — it goes in the
   YouTube **description** and/or a link to the song's sheet (two learner flows:
   study-first or listen-first). See `video-format.md`. Possible enhancement:
   `build_sheets.mjs` can emit a `description.txt` / clean learner list — decide
   when the first video ships.
3. **Generate `01-aiueo` v3 audio** with the **city-pop female Persona** (~3:00;
   Extend if needed) → `learning/01-aiueo/` → `tools/align` → the sing-along video
   → export (YouTube + vertical). The visuals (task 1) can be prototyped on the
   existing placeholder timing before the real audio lands.
4. **Make the Persona** from the winning city-pop take; record its exact sliders in
   `findings.md` F-11 (the recipe still lacks precise weirdness/style-influence).
5. Also open: **sting + sign-off** into `assets/`; the **gap test** on
   `01-aisatsu` + a `screen.txt` for it; optionally migrate `01/02` to full
   `song.json`.

---

# 2026-08-27 (session 2) — AIUEO becomes a real video: engine, alignment, brand

Previous: the genesis section above.

## What happened
- **Learning-video template built** (`video/src/LearningVideo.tsx`) — a new Remotion
  composition on the same parametric registry as the lullaby, but its own world:
  - **Two-line ping-pong karaoke** in fixed top/bottom slots (read top→bottom→top,
    subtle fade, no scroll); current line fills red / gold on refrains, next line
    previews in grey; `fitText` keeps any line to two lines.
  - **Illustration bands** (top + bottom, bottom offset by one), tiled small with a
    papercraft sway + beat pulse.
  - **Reactive centre-divider waveform** from the raw audio samples
    (`useAudioData().channelWaveforms`), dark-cream, edge-to-edge.
  - **CD-player header**: top-left `01 ▶ あいうえお` (row, hiragana) · centre song
    name (`ちいさな いちにち`) · top-right ICONOTES symbol logo; bottom-left
    `City Pop · 92 BPM` · centre progress bar + time · bottom-right `©iconotes`.
  - Per-line `delay` + global `offsetSeconds` timing levers; dots count-in.
- **WhisperX alignment** on the real 3-min city-pop `.wav` (the CORRECT take — the
  first mp3 was the wrong song): **339/339 chars anchored**. Vocals span
  ~1.3–146.5 s of 179.5 s (a **~33 s instrumental tail** still to decide on).
- **Irasutoya illustration pipeline** (`tools/illustrations/`): manifest →
  `q.py` semantic search → `fetch.mjs` (copies into git-ignored `video/public/il/`,
  enforces the **18-unique cap**) → `contact.mjs` sheet → `credits.mjs` for the
  description. 15 single-item picks for AIUEO.
- **Site retheme** to **dark-navy + red** across the dashboard, sheets, glossary,
  and docs (one committed world).
- **ICONOTES logo** recreated as a design-canvas Artifact; the brand's own symbol
  SVG now sits in the video, tinted to the corner tone.
- **Structural cleanup**: `01-aisatsu` and `02-gojuon` were tests → moved to
  git-ignored `personal/` (removed from the public repo/dashboard). The AIUEO song
  renamed `03-aiueo` → **`01-aiueo`** (episode 1). Dashboard now lists one song.

## Decisions
- **Learning template ≠ lullaby**: separate `LearningVideo`; cream is its fixed
  identity, night-sky stays the lullaby's.
- **Waveform = real audio, subtle, centre divider** (not a bar equalizer).
- **Illustrations only in the two bands**, repeated; single-item icons over
  compositions; **≤18 unique per video** (Irasutoya limit, enforced by `fetch.mjs`).
- **Song name approved**: `ちいさな いちにち` (hiragana). Track number `01`. **Mee logo: not used.**

## State now
- **AIUEO video renders end-to-end** (`npx remotion render 01-aiueo`): real audio,
  aligned karaoke, real illustrations, full branded header.
- `learning/01-aiueo/` is the only public song; `01-aisatsu`/`02-gojuon` are local-only.
- Audio, images, logo SVGs, renders all git-ignored; privacy checks pass.

## Next (where to pick up)
1. **Verify sync by ear**; nudge with `offsetSeconds` (whole song) or per-line
   `delay` in `video/timing/01-aiueo.learning.json` (Studio: `cd video && npm start`).
2. **Decide the ~33 s instrumental tail** (146.5→179.5 s): outro, trim, or outro card.
3. **Render final full-res** (`--scale=1 --crf=16`) → export YouTube + vertical cut.
4. Optional polish: sway/pulse amount, waveform amplitude, media-player sizing.
5. Still open from genesis: **Persona** sliders in F-11; **sting + sign-off** in `assets/`.

## Update (same session) — full-res masters + vertical cut
- **Rendered the final masters** (crf 16, git-ignored under `video/out/`):
  `01-aiueo.mp4` (1920×1080) and `01-aiueo-vertical.mp4` (1080×1920), ~3 min each
  (both ~85 MB — over the chat send limit, so they live on disk).
- **Template is now aspect-aware** (`LearningVideo.tsx`): sizing keys off the SHORT
  edge, and the illustration bands drop to **3 tiles** in portrait. Added a
  `01-aiueo-vertical` composition (1080×1920) + `calculateLearningMetadataVertical`.
  Landscape output is unchanged (short edge = height there). Render targets:
  `npx remotion render 01-aiueo` and `… 01-aiueo-vertical`.

### Next (picking up — the vertical needs a little more)
1. **Tune the portrait footer** — at 1080 wide the progress bar crowds `92 BPM`;
   shrink the corner text or stack/narrow the bar, then re-render the vertical.
2. **Other vertical adjustments** the user has in mind (TBD next session).
3. Optional: a **~1-min Shorts/Reels cut** (PART 1 only) = a separate trimmed timing.
4. Carried over before publish: verify **sync** by ear (`offsetSeconds` /
   per-line `delay` in `video/timing/01-aiueo.learning.json`), and decide the
   **~33 s instrumental tail** (146.5→179.5 s: outro / trim / card).
