# 04-tachitsuteto-ouchi — た行, the Home variation

Gojuon **row 4 (た ち つ て と)**, taught through a day at home — an egg and tea, a desk and
a letter, blocks and a drum, the clock and the night moon. The **home** variation of three
た-row songs (home · `shizen` nature · `doubutsu` animals); た行 is rich in everyday objects.

**Status: lyrics drafted (stage 1–2).** No audio yet. Written 2026-08-28. Recombines たべる,
のむ, みる, あらう, ごはん, あかい — the F-13 ladder. Pairs with the **Female (city pop)** voice.

## Files
- `song.json` — single source of truth. `screen.txt` — on-screen lines (all-kana).
- `learner.txt` — study sheet. `suno-v2/v3.txt` — Suno scores. `style.txt` — voice notes.

## Next (see `../../PIPELINE.md` + `../../PROJECT-STRUCTURE.md`)
Generate in Suno from `suno-v3.txt` (Persona: Female JP City Pop) → pick take →
`audio/master.wav`, fetch `il/` → `build_learning_timing 04-tachitsuteto-ouchi` → align →
`gen_registry` → `sync` → render → `gen_description`.
