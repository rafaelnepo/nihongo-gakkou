# 07-mamimumemo-ouchi — ま行, the Home variation

Gojuon **row 7 (ま み む め も)**, taught through a cosy day at home — open the window, eat
beans, a sweet mandarin, round glasses, a sweet peach and white mochi under a warm blanket.
The **home** variation of three ま-row songs (home · `shizen` nature · `doubutsu` animals).

**Status: lyrics drafted (stage 1–2).** No audio yet. Written 2026-08-28. Recombines あける,
たべる, うさぎ — the F-13 ladder. Pairs with the **Female (city pop)** voice. Uses を correctly
(を stays を, never swapped — findings F-16).

## Files
- `song.json` — single source of truth. `screen.txt` — on-screen lines (all-kana).
- `learner.txt` — study sheet. `suno-v2/v3.txt` — Suno scores. `style.txt` — voice notes.

## Next (see `../../PIPELINE.md` + `../../PROJECT-STRUCTURE.md`)
Generate in Suno from `suno-v3.txt` (Persona: Female JP City Pop) → pick take →
`audio/master.wav`, fetch `il/` → `build_learning_timing 07-mamimumemo-ouchi` → align →
`gen_registry` → `sync` → render → `gen_description`.
