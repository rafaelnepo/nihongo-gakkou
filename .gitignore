# ── PRIVACY: the family songs never enter the repo / GitHub / Pages ──
personal/

# ── Reference library — copyrighted books / notes, kept local only ──
books/

# ── Private song timing (family lullaby lyrics) — kept local, engine uses aiueo-demo ──
video/timing/onaji-tsuki.json

# ── Heavy, reproducible ──
node_modules/
tools/align/.venv/
__pycache__/
*.pyc
video/out/
.remotion/

# ── Media (audio/video) — large, regenerated from Suno/Remotion, not source ──
*.wav
*.mp3
*.mp4
*.m4a
video/public/*
assets/            # local Suno exports (art/video), kept out of the repo

# ── OS / editor ──
.DS_Store
*.log

# keep the public/ folder in the repo (audio itself stays ignored)
!video/public/.gitkeep
